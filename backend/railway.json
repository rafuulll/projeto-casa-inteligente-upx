{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma db push && node index.js",
    "healthcheckPath": "/api/telemetria/atual",
    "restartPolicyType": "ON_FAILURE"
  }
}
